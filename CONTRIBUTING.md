# Contributing

Issues and focused pull requests are welcome. Keep the tool surface small, make billable behavior explicit, and include tests for any protocol or prompt change.

```bash
pnpm install
pnpm check
```

Do not commit API keys, OAuth tokens, generated credentials, or user data.
